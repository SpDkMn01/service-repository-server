spring:
  application:
    name: ms-payment
  data:
    mongodb:
      database: bootcamp_payment_db
      host: db_payment
      port: 27017
management:
  endpoints:
    web:
      exposure:
        include: "*"
server:
  port: 8000
message:
  path-payment: /api/v1/payment
  uri: /api/v1/customerProductsActive/